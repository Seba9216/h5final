import { DuckingLoginModel } from './ducking-login.model';

describe('DuckingLoginModel', () => {
  it('should create an instance', () => {
    expect(new DuckingLoginModel()).toBeTruthy();
  });
});
