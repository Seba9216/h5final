import { RaceFinishDirective } from './race-finish-directive';

describe('RaceFinishDirective', () => {
  it('should create an instance', () => {
    const directive = new RaceFinishDirective();
    expect(directive).toBeTruthy();
  });
});
